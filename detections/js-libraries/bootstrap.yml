name: Bootstrap
category: JS Library
website: https://getbootstrap.com
checks:
  body:
    patterns:
      - pattern: "bootstrap.min.css"
      - pattern: "bootstrap.min.js"
  js:
    - expression: "bootstrap.Alert.VERSION"
      version: true
    - expression: "jQuery.fn.alert.Constructor.VERSION"
